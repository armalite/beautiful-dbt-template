{
    "PROJECT_NAME": "Example Project Name",
    "__PROJECT_SLUG": "{{ cookiecutter.PROJECT_NAME.lower().replace(' ', '-') }}",

    "DESCRIPTION": "Enter a short description...",

    "AUTHOR_NAME": "Enter your first name and last name...",
    "USER_NAME": "Enter your Snowflake Username",
    "WORKSPACE": "YOURWORKSPACE",
    "__WORKSPACE_SANDBOX": "{{ cookiecutter.WORKSPACE.upper() }}_DEV",
    "__WORKSPACE_SANDBOX_SA": "{{ cookiecutter.WORKSPACE.upper() }}_DEV_SA",
    "__WORKSPACE_SANDBOX_ROLE": "{{ cookiecutter.WORKSPACE.upper() }}_DEV_ADMIN",
    "__WORKSPACE_SANDBOX_DB": "{{ cookiecutter.WORKSPACE.upper() }}_DEV",
    "__WORKSPACE_SANDBOX_WH": "{{ cookiecutter.WORKSPACE.upper() }}_DEV_WH",
    "__WORKSPACE_LAB": "{{ cookiecutter.WORKSPACE.upper() }}_UAT",
    "__WORKSPACE_LAB_SA": "{{ cookiecutter.WORKSPACE.upper() }}_UAT_SA",
    "__WORKSPACE_LAB_ROLE" : "{{ cookiecutter.WORKSPACE.upper() }}_UAT_ADMIN",
    "__WORKSPACE_LAB_DB": "{{ cookiecutter.WORKSPACE.upper() }}_UAT",
    "__WORKSPACE_LAB_WH": "{{ cookiecutter.WORKSPACE.upper() }}_UAT_WH",
    "__WORKSPACE_HWAY": "{{ cookiecutter.WORKSPACE.upper() }}_PROD",
    "__WORKSPACE_HWAY_SA": "{{ cookiecutter.WORKSPACE.upper() }}_PROD_SA",
    "__WORKSPACE_HWAY_ROLE" : "{{ cookiecutter.WORKSPACE.upper() }}_PROD_ADMIN",
    "__WORKSPACE_HWAY_DB": "{{ cookiecutter.WORKSPACE.upper() }}_PROD",
    "__WORKSPACE_HWAY_WH": "{{ cookiecutter.WORKSPACE.upper() }}_PROD_WH",

    "_SCHEMA_CI": "CI_{{ env_var('BUILD_BRANCH') | upper }}",
    "_SCHEMA_LOCAL": "DEV_{{ cookiecutter.USERNAME }}",
    "_USER_LOCAL": "{{ cookiecutter.USERNAME }}",
    "_TARGET_DEFAULT": "{{ env_var('DBT_TARGET', 'dev') }}",
    "_KEY_PATH": "private.key"
}